# PSHueServerCheck
Powershell that check server status, and turns a Philips Hue LED Strip red if there is any errors


Uses Raspberry Pi with pwsh to execute using a cronjob (must be sudo crontab -e)

Requires the Test-TCPConnection PS module installed.
